#include<stdio.h>
#include<conio.h>
#include<iostream>
using namespace std;
int fact(int a)
{
	if(a==1)
	{
		return 1;
	}
	else
	{
		return (a*fact(a-1));
	}
}
int main()
{
	int a;
	cout<<"Enter the number";
	cin>>a;
	int c=fact(a);
	cout<<c;
}
